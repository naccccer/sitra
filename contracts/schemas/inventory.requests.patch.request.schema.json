{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "InventoryRequestsPatchRequest",
  "type": "object",
  "required": ["id", "action"],
  "properties": {
    "id": { "type": ["string", "number"] },
    "action": { "type": "string", "enum": ["approve", "reject", "cancel"] },
    "resolutionNotes": { "type": "string" }
  }
}
